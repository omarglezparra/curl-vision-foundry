EXPO_PUBLIC_CAPTURE_API_BASE=https://your-function-app.azurewebsites.net/api

# Enter AZURE_INGEST_API_TOKEN (upload) and AZURE_MINIAPP_API_TOKEN (dashboard)
# inside the app. Both are saved with SecureStore and must not be embedded in
# EXPO_PUBLIC variables or committed to source.
